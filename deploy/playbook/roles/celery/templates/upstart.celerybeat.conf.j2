# simple celerybeat upstart job

description "{{ project.name }}-celerybeat instance"
start on runlevel [2345]
stop on runlevel [06]


exec {{ project.virtualenv }}bin/python {{ django.root }}manage.py celery beat --loglevel=INFO --settings=base.settings.live
